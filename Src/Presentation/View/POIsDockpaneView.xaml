<UserControl
    x:Class="EAABAddIn.Src.Presentation.View.POIsDockpaneView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
    mc:Ignorable="d"
    d:DesignHeight="320"
>
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <extensions:DesignOnlyResourceDictionary Source="pack://application:,,,/ArcGIS.Desktop.Framework;component\Themes\Default.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <BooleanToVisibilityConverter x:Key="BoolToVis" />

            <Style x:Key="HeaderTextStyle" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="Foreground" Value="{DynamicResource Esri_TextPrimaryBrush}" />
                <Setter Property="Margin" Value="0,0,0,5" />
            </Style>

            <Style x:Key="ThemedTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                <Setter Property="Background" Value="{DynamicResource Esri_BackgroundContentBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource Esri_TextPrimaryBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Esri_BorderBrush}" />
                <Setter Property="Height" Value="28" />
                <Setter Property="Padding" Value="8,4" />
                <Setter Property="Margin" Value="0,0,0,15" />
                <Setter Property="FontSize" Value="13" />
            </Style>

            <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource Esri_Button}">
                <Setter Property="Background" Value="{DynamicResource Esri_Blue}" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Height" Value="32" />
                <Setter Property="Padding" Value="16,8" />
                <Setter Property="FontWeight" Value="Medium" />
                <Setter Property="Cursor" Value="Hand" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource Esri_BlueHover}" />
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Background" Value="{DynamicResource Esri_Gray155}" />
                        <Setter Property="Foreground" Value="{DynamicResource Esri_Gray120}" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="12" x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border
            Grid.Row="0"
            CornerRadius="6"
            Background="{DynamicResource Esri_BackgroundContentBrush}"
            Padding="10"
            BorderBrush="{DynamicResource Esri_BorderBrush}"
            BorderThickness="1"
            Margin="0,0,0,8"
        >
            <StackPanel Orientation="Vertical">
                <TextBlock Text="Buscar POIs" Style="{StaticResource HeaderTextStyle}" Margin="0,0,0,6" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Grid Margin="0,0,8,0">
                        <TextBox
                            x:Name="SearchBox"
                            Style="{StaticResource ThemedTextBoxStyle}"
                            Text="{Binding SearchInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            Margin="0"
                            VerticalContentAlignment="Center"
                        />
                        <TextBlock
                            Text="Ingrese término (mín 2 caracteres)"
                            Foreground="{DynamicResource Esri_TextSecondaryBrush}"
                            IsHitTestVisible="False"
                            Margin="10,0,6,0"
                            VerticalAlignment="Center"
                            FontSize="12"
                            Visibility="{Binding ElementName=SearchBox, Path=Text, Converter={StaticResource BoolToVis}, ConverterParameter=empty}"
                        />
                    </Grid>

                    <Button Grid.Column="1" Content="Buscar" MinWidth="78" Height="30" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding SearchCommand}" />
                </Grid>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,4,0,4">
            <TextBlock Text="Resultados:" Style="{StaticResource HeaderTextStyle}" Margin="0,0,12,0" />
            <TextBlock Text="{Binding Status}" VerticalAlignment="Center" Style="{DynamicResource Esri_TextBlockRegular}" />
        </StackPanel>

        <Border Grid.Row="2" CornerRadius="6" BorderBrush="{DynamicResource Esri_BorderBrush}" BorderThickness="1" Background="{DynamicResource Esri_BackgroundContentBrush}" Padding="0" Margin="0,0,0,8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <DataGrid
                    ItemsSource="{Binding Results}"
                    SelectedItem="{Binding Selected}"
                    AutoGenerateColumns="False"
                    CanUserAddRows="False"
                    CanUserDeleteRows="False"
                    IsReadOnly="True"
                    SelectionMode="Single"
                    RowHeaderWidth="0"
                    Margin="0"
                    GridLinesVisibility="None"
                    HeadersVisibility="Column"
                    AlternationCount="2"
                    EnableRowVirtualization="True"
                    HorizontalScrollBarVisibility="Auto"
                    VerticalScrollBarVisibility="Auto"
                    Style="{DynamicResource Esri_DataGrid}"
                >
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="POI" Binding="{Binding NamePoi}" Width="2*" />
                        <DataGridTextColumn Header="Dirección" Binding="{Binding Address}" Width="3*" />
                        <DataGridTextColumn Header="Ciudad" Binding="{Binding CityDesc}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <WrapPanel Grid.Row="3" HorizontalAlignment="Right" ItemHeight="32" ItemWidth="Auto" Margin="0,2,0,0">
            <Button Content="Agregar" MinWidth="88" Height="32" Style="{StaticResource Esri_Button}" Command="{Binding MarkSelectedCommand}" Margin="0,0,8,0" />
            <Button Content="Agregar Todos" MinWidth="110" Height="32" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding MarkAllCommand}" Margin="0" />
        </WrapPanel>
    </Grid>
</UserControl>