<UserControl
    x:Class="EAABAddIn.Src.Presentation.View.POIsDockpaneView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
    xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"
    xmlns:viewModel="clr-namespace:EAABAddIn.Src.Presentation.ViewModel"
    xmlns:view="clr-namespace:EAABAddIn.Src.Presentation.View"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DataContext="{Binding Path=viewModel.POIsDockpaneViewModel}"
    MinWidth="250"
>
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <extensions:DesignOnlyResourceDictionary Source="pack://application:,,,/ArcGIS.Desktop.Framework;component\Themes\Default.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <BooleanToVisibilityConverter x:Key="BoolToVis" />

            <Style x:Key="HeaderTextStyle" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="Foreground" Value="{DynamicResource Esri_TextPrimaryBrush}" />
                <Setter Property="Margin" Value="0,0,0,5" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>

            <Style x:Key="ThemedTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                <Setter Property="Background" Value="{DynamicResource Esri_BackgroundContentBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource Esri_TextPrimaryBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Esri_BorderBrush}" />
                <Setter Property="Height" Value="28" />
                <Setter Property="Padding" Value="8,4" />
                <Setter Property="Margin" Value="0,0,0,8" />
                <Setter Property="FontSize" Value="13" />
            </Style>

            <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource Esri_Button}">
                <Setter Property="Background" Value="{DynamicResource Esri_Blue}" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="MinHeight" Value="32" />
                <Setter Property="Padding" Value="12,6" />
                <Setter Property="FontWeight" Value="Medium" />
                <Setter Property="Cursor" Value="Hand" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource Esri_BlueHover}" />
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Background" Value="{DynamicResource Esri_Gray155}" />
                        <Setter Property="Foreground" Value="{DynamicResource Esri_Gray120}" />
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="ResponsiveButtonStyle" TargetType="Button" BasedOn="{StaticResource Esri_Button}">
                <Setter Property="MinHeight" Value="32" />
                <Setter Property="Padding" Value="8,6" />
                <Setter Property="Margin" Value="0,0,4,0" />
                <Setter Property="HorizontalAlignment" Value="Stretch" />
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="10,14,10,14">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Sección de búsqueda -->
            <Border Grid.Row="0"
                CornerRadius="6"
                Background="{DynamicResource Esri_BackgroundContentBrush}"
                Padding="10"
                BorderBrush="{DynamicResource Esri_BorderBrush}"
                BorderThickness="1"
                Margin="0,0,0,8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                        Text="Buscar POIs"
                        Style="{StaticResource HeaderTextStyle}"
                        Margin="0,0,0,8"
                    />

                    <Grid Grid.Row="1" Margin="0,0,0,8">
                        <TextBox
                            x:Name="SearchBox"
                            Style="{StaticResource ThemedTextBoxStyle}"
                            Text="{Binding SearchInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            Margin="0"
                        />

                        <TextBlock Text="Ingrese término (mín 2 caracteres)"
                            Foreground="{DynamicResource Esri_TextSecondaryBrush}"
                            IsHitTestVisible="False"
                            Margin="10,0,6,8"
                            VerticalAlignment="Center"
                            Visibility="{Binding ElementName=SearchBox, Path=Text, Converter={StaticResource BoolToVis}, ConverterParameter=empty}"
                        />
                    </Grid>

                    <Button Grid.Row="2"
                        Content="Buscar"
                        HorizontalAlignment="Stretch"
                        Height="32"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding SearchCommand}"
                    />
                </Grid>
            </Border>

            <StackPanel Grid.Row="1"
                Orientation="Vertical"
                Margin="0,4,0,8"
            >
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Resultados:"
                        Style="{StaticResource HeaderTextStyle}"
                        Margin="0,0,8,0"
                    />

                    <TextBlock Text="{Binding Status}"
                        VerticalAlignment="Center"
                        Foreground="{DynamicResource Esri_TextSecondaryBrush}"
                        TextWrapping="Wrap"
                    />
                </StackPanel>
            </StackPanel>

            <Border Grid.Row="2"
                CornerRadius="6"
                BorderBrush="{DynamicResource Esri_BorderBrush}"
                BorderThickness="1"
                Background="{DynamicResource Esri_BackgroundContentBrush}"
                Padding="4"
                Margin="0,0,0,12"
            >
                <DataGrid
                    ItemsSource="{Binding Results}"
                    SelectedItem="{Binding Selected}"
                    AutoGenerateColumns="False"
                    CanUserAddRows="False"
                    CanUserDeleteRows="False"
                    IsReadOnly="True"
                    SelectionMode="Single"
                    RowHeaderWidth="0"
                    Margin="0"
                    MinHeight="140"
                    MaxHeight="300"
                    GridLinesVisibility="Horizontal"
                    HeadersVisibility="Column"
                    AlternationCount="2"
                    HorizontalScrollBarVisibility="Auto"
                    VerticalScrollBarVisibility="Auto"
                    Style="{DynamicResource Esri_DataGrid}"
                >
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="POI" Binding="{Binding NamePoi}" Width="1.5*" MinWidth="80" />
                        <DataGridTextColumn Header="Dirección" Binding="{Binding Address}" Width="2*" MinWidth="100" />
                        <DataGridTextColumn Header="Ciudad" Binding="{Binding CityDesc}" Width="*" MinWidth="70" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <StackPanel Grid.Row="3" Orientation="Vertical">
                <Button Content="Agregar"
                    Height="36"
                    HorizontalAlignment="Stretch"
                    Style="{StaticResource ResponsiveButtonStyle}"
                    Command="{Binding MarkSelectedCommand}"
                    Margin="0,0,0,8"
                />

                <Button Content="Agregar Todos"
                    Height="36"
                    HorizontalAlignment="Stretch"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Command="{Binding MarkAllCommand}"
                    Margin="0"
                />
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>