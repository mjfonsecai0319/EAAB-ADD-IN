<UserControl
    x:Class="EAABAddIn.Src.Presentation.View.MassiveGeocodeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
    xmlns:conv="clr-namespace:EAABAddIn.Src.Presentation.Converters"
    mc:Ignorable="d"
    d:DesignHeight="800"
    d:DesignWidth="450"
>
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <extensions:DesignOnlyResourceDictionary
                    Source="pack://application:,,,/ArcGIS.Desktop.Framework;component/Themes/Default.xaml"
                />
            </ResourceDictionary.MergedDictionaries>

            <conv:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
            <conv:InverseBooleanConverter x:Key="InverseBooleanConverter" />

            <Style x:Key="HeaderTextStyle" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="Foreground" Value="{DynamicResource Esri_TextPrimaryBrush}" />
                <Setter Property="Margin" Value="0,0,0,5" />
            </Style>

            <Style x:Key="BrowseButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Background" Value="{DynamicResource Esri_BackgroundHoverBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource Esri_TextStyleEmphasisBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Esri_BorderBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="Cursor" Value="Hand" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource Esri_BackgroundPressedBrush}" />
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="StatusTextStyle" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                <Setter Property="FontWeight" Value="Medium" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="Foreground" Value="{DynamicResource Esri_TextSecondaryBrush}" />
            </Style>

            <Style x:Key="InfoTextStyle" TargetType="TextBlock" BasedOn="{StaticResource StatusTextStyle}">
                <Setter Property="TextWrapping" Value="Wrap" />
                <Setter Property="LineHeight" Value="16" />
                <Setter Property="Margin" Value="0,0,0,10" />
            </Style>

            <Style x:Key="ConnectionStatusBorderStyle" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource Esri_DialogFrameBackgroundBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Esri_Blue}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="4" />
                <Setter Property="Padding" Value="15" />
                <Setter Property="Margin" Value="0,20,0,0" />
            </Style>

            <Style x:Key="InstructionsContainerStyle" TargetType="Border" BasedOn="{StaticResource ConnectionStatusBorderStyle}">
                <Setter Property="BorderBrush" Value="{DynamicResource Esri_Blue}" />
                <Setter Property="Padding" Value="15" />
                <Setter Property="Margin" Value="0,15,0,20" />
            </Style>

            <Style x:Key="ThemedProgressBarStyle" TargetType="ProgressBar" BasedOn="{StaticResource {x:Type ProgressBar}}">
                <Setter Property="Background" Value="{DynamicResource Esri_BackgroundPressedBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource Esri_Blue}" />
                <Setter Property="Height" Value="4" />
                <Setter Property="Margin" Value="0,8,0,0" />
            </Style>

            <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource Esri_Button}">
                <Setter Property="Background" Value="{DynamicResource Esri_Blue}" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Height" Value="32" />
                <Setter Property="Padding" Value="16,8" />
                <Setter Property="FontWeight" Value="Medium" />
                <Setter Property="Cursor" Value="Hand" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource Esri_BlueHover}" />
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Background" Value="{DynamicResource Esri_Gray155}" />
                        <Setter Property="Foreground" Value="{DynamicResource Esri_Gray120}" />
                    </Trigger>
                </Style.Triggers>
            </Style>

        </ResourceDictionary>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <TextBlock
                    Text="Seleccionar archivo Excel:"
                    Style="{StaticResource HeaderTextStyle}"
                    Margin="0,0,0,10"
                />

                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="10" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBox
                        Grid.Column="0"
                        Text="{Binding FileInput, UpdateSourceTrigger=PropertyChanged}"
                        ToolTip="{Binding FileInput}"
                        IsReadOnly="True"
                        Height="28"
                    />

                    <Button
                        Grid.Column="2"
                        Style="{StaticResource BrowseButtonStyle}"
                        Command="{Binding OpenFileDialogCommand}"
                        Height="28"
                        Width="28"
                    >
                        <Image Width="16" Height="16" Source="pack://application:,,,/EAABAddIn;component/Images/FolderIcon16.png" />
                    </Button>
                </Grid>

                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="5" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Text="Seleccione la GDB:" Style="{StaticResource HeaderTextStyle}" Grid.Column="0" Grid.Row="0" />

                    <TextBox
                        x:Name="GDBPathBox"
                        Text="{Binding GdbPath}"
                        FontFamily="Segoe UI"
                        IsReadOnly="True"
                        Grid.Column="0"
                        Grid.Row="1"
                        Height="28"
                        Margin="0,0,0,0"
                    />

                    <Button
                        x:Name="BrowseButton"
                        Grid.Column="2"
                        Grid.Row="1"
                        Style="{StaticResource BrowseButtonStyle}"
                        Height="28"
                        Width="28"
                        Command="{Binding BrowseGdbCommand}"
                    >
                        <Image Width="16" Height="16" Source="pack://application:,,,/EAABAddIn;component/Images/FolderIcon16.png" />
                    </Button>
                </Grid>

                <Border Style="{StaticResource InstructionsContainerStyle}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource InfoTextStyle}">
                            <Run Text="El archivo debe ser un Excel (.xlsx) con las columnas:" />
                            <LineBreak />
                            <Run Text="   • " />
                            <Run Text="Identificador" FontWeight="Medium" />
                            <Run Text=" - ID único del registro (opcional pero recomendado)" />
                            <LineBreak />
                            <Run Text="   • " />
                            <Run Text="Direccion" FontWeight="Medium" />
                            <Run Text=" - Dirección a geocodificar" />
                            <LineBreak />
                            <Run Text="   • " />
                            <Run Text="Poblacion" FontWeight="Medium" />
                            <Run Text=" - Código de ciudad (NO el nombre)." />
                            <LineBreak />
                        </TextBlock>

                        <StackPanel>
                            <TextBlock Style="{StaticResource InfoTextStyle}" TextWrapping="Wrap" Foreground="{DynamicResource Esri_TextPrimaryBrush}" Text="Códigos de ciudad (muestra):" />
                            <Border BorderBrush="{DynamicResource Esri_BorderBrush}" BorderThickness="1" CornerRadius="2" Padding="4" Background="{DynamicResource Esri_DialogFrameBackgroundBrush}">
                                <ScrollViewer MaxHeight="150" VerticalScrollBarVisibility="Auto">
                                    <ItemsControl ItemsSource="{Binding CityCodes}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,2">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="100" />
                                                        <ColumnDefinition Width="10" />
                                                        <ColumnDefinition Width="*" />
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="{Binding CityCode}" FontWeight="SemiBold" />
                                                    <TextBlock Grid.Column="2" Text="{Binding CityDesc}" TextTrimming="CharacterEllipsis" />
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </Border>
                        </StackPanel>

                    </StackPanel>
                </Border>
            </StackPanel>

            <Border Grid.Row="1" Style="{StaticResource InstructionsContainerStyle}">
                <StackPanel>
                    <TextBlock
                        Text="Proceso de Geocodificación:"
                        Style="{StaticResource HeaderTextStyle}"
                        Foreground="{DynamicResource Esri_Blue}"
                        Margin="0,0,0,8"
                    />
                    <TextBlock Style="{StaticResource InfoTextStyle}">
                        <Run Text="1. Seleccione un archivo Excel válido" />
                        <LineBreak />
                        <Run Text="2. El sistema validará el formato y las columnas" />
                        <LineBreak />
                        <Run Text="3. Se procesarán todas las direcciones automáticamente" />
                        <LineBreak />
                        <Run Text="4. Los resultados se mostrarán en el mapa" />
                    </TextBlock>
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="3" Margin="0,10,0,15">
                <TextBlock
                    Text="{Binding StatusMessage}"
                    Style="{StaticResource StatusTextStyle}"
                    FontWeight="Medium"
                    TextWrapping="Wrap"
                    Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                />

                <ProgressBar
                    Style="{StaticResource ThemedProgressBarStyle}"
                    IsIndeterminate="True"
                    Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                />

                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                    <Button
                        Content="Procesar Archivo"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding SearchCommand}"
                        IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"
                    />
                </StackPanel>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>