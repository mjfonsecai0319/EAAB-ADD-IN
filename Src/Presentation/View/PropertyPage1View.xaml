<UserControl x:Class="EAABAddIn.Src.Presentation.View.PropertyPage1View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:EAABAddIn.Src.Presentation.ViewModel"
             xmlns:conv="clr-namespace:EAABAddIn.Src.Presentation.Converters"
             mc:Ignorable="d">

    <UserControl.Resources>
        <conv:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <UserControl.DataContext>
        <vm:PropertyPage1ViewModel/>
    </UserControl.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Motor:" Width="120" VerticalAlignment="Center"/>
            <ComboBox Width="200"
                      ItemsSource="{Binding MotoresBD}"
                      SelectedItem="{Binding MotorSeleccionado, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,5" Grid.Row="1">
            <TextBlock Text="Host:" Width="120" VerticalAlignment="Center"/>
            <TextBox Width="200"
                     Text="{Binding Host, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,5" Grid.Row="2">
            <TextBlock Text="Puerto:" Width="120" VerticalAlignment="Center"/>
            <TextBox Width="200"
                     Text="{Binding Puerto, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,5" Grid.Row="3">
            <TextBlock Text="Base de Datos:" Width="120" VerticalAlignment="Center"/>
            <TextBox Width="200"
                     Text="{Binding BaseDeDatos, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,5" Grid.Row="4">
            <TextBlock Text="Usuario:" Width="120" VerticalAlignment="Center"/>
            <TextBox Width="200"
                     Text="{Binding Usuario, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,5" Grid.Row="5">
            <TextBlock Text="Contraseña:" Width="120" VerticalAlignment="Center"/>
            <PasswordBox Width="200"
                         PasswordChanged="PasswordBox_PasswordChanged"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,5" Grid.Row="6"
                    Visibility="{Binding MotorSeleccionado,
                                         Converter={StaticResource BooleanToVisibilityConverter},
                                         ConverterParameter=Oracle}">
            <TextBlock Text="Oracle Path:" Width="120" VerticalAlignment="Center"/>
            <TextBox Width="200"
                     Text="{Binding OraclePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <Button Grid.Row="7"
                Content="Probar Conexión"
                Width="150" Height="30" Margin="0,10,0,0"
                Command="{Binding ProbarConexionCommand}"/>

        <TextBlock Grid.Row="8"
                   Text="{Binding MensajeConexion}"
                   FontWeight="Bold"
                   Foreground="DarkRed"
                   Margin="0,10,0,0"/>
    </Grid>
</UserControl>
