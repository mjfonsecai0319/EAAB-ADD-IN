<UserControl
    x:Class="EAABAddIn.Src.Presentation.View.MigrationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
    xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"
    xmlns:viewModel="clr-namespace:EAABAddIn.Src.Presentation.ViewModel"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DataContext="{Binding Path=viewModel.MigrationViewModel}"
    MinWidth="250"
>
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <extensions:DesignOnlyResourceDictionary Source="pack://application:,,,/ArcGIS.Desktop.Framework;component\Themes\Default.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="HeaderTextStyle" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="Foreground" Value="{DynamicResource Esri_TextPrimaryBrush}" />
                <Setter Property="Margin" Value="0,0,0,6" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="10,12,10,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Migración de datos" Style="{StaticResource HeaderTextStyle}" />
        <TextBlock Grid.Row="1" Text="Este panel permitirá migrar entidades entre capas. Próximamente." Foreground="{DynamicResource Esri_TextSecondaryBrush}" />

        <Border Grid.Row="2" Margin="0,8,0,0" Padding="8" BorderBrush="{DynamicResource Esri_BorderBrush}" BorderThickness="1" Background="{DynamicResource Esri_BackgroundContentBrush}">
            <StackPanel>
                <TextBlock Text="Sugerencia:" FontWeight="SemiBold"/>
                <TextBlock Text="Seleccione la capa origen y destino en el mapa y use este panel cuando se habilite la migración." TextWrapping="Wrap"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
