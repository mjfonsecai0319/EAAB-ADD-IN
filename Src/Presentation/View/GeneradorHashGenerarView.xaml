<UserControl x:Class="EAABAddIn.Src.Presentation.View.GeneradorHashGenerarView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
    mc:Ignorable="d"
    d:DesignHeight="600" d:DesignWidth="400"
>
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <extensions:DesignOnlyResourceDictionary
                    Source="pack://application:,,,/ArcGIS.Desktop.Framework;component\Themes\Default.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

            <Style x:Key="HeaderTextStyle" TargetType="TextBlock"
                BasedOn="{StaticResource {x:Type TextBlock}}">
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Foreground" Value="{DynamicResource Esri_TextPrimaryBrush}" />
                <Setter Property="Margin" Value="0,0,0,8" />
            </Style>

            <Style x:Key="PrimaryButtonStyle" TargetType="Button"
                BasedOn="{StaticResource Esri_Button}">
                <Setter Property="Background" Value="{DynamicResource Esri_Blue}" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Height" Value="36" />
                <Setter Property="Padding" Value="16,8" />
                <Setter Property="FontWeight" Value="Medium" />
                <Setter Property="Cursor" Value="Hand" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource Esri_BlueHover}" />
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Background" Value="{DynamicResource Esri_Gray155}" />
                        <Setter Property="Foreground" Value="{DynamicResource Esri_Gray120}" />
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="SecondaryButtonStyle" TargetType="Button"
                BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{DynamicResource Esri_Blue}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Esri_Blue}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Height" Value="36" />
                <Setter Property="Padding" Value="16,8" />
                <Setter Property="FontWeight" Value="Medium" />
                <Setter Property="Cursor" Value="Hand" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource Esri_BackgroundHoverBrush}" />
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="ResultBoxStyle" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource Esri_BackgroundPressedBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Esri_BorderBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="12" />
                <Setter Property="CornerRadius" Value="4" />
                <Setter Property="Margin" Value="0,8,0,0" />
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="15">
            <!-- Selector de Funcionalidad -->
            <TextBlock Text="Funcionalidad *" Style="{StaticResource HeaderTextStyle}" />
            <ComboBox SelectedIndex="{Binding FuncionalidadSeleccionada, Mode=TwoWay}"
                      Height="32"
                      Margin="0,0,0,15">
                <ComboBoxItem Content="Comprimir GDB y Generar Hash" />
                <ComboBoxItem Content="Generar Hash de Archivos en Carpeta" />
            </ComboBox>

            <!-- Ruta de Carpeta/GDB -->
            <TextBlock Text="Carpeta/GDB *" Style="{StaticResource HeaderTextStyle}" />
            <Grid Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Text="{Binding RutaCarpetaGenerar, Mode=TwoWay}"
                         Height="32"
                         VerticalContentAlignment="Center"
                         Padding="8,0" />

                <Button Grid.Column="2"
                        Command="{Binding ExaminarCarpetaCommand}"
                        Height="32"
                        Width="32"
                        ToolTip="Examinar..."
                        Style="{DynamicResource Esri_Button}">
                    <TextBlock Text="..." FontSize="16" FontWeight="Bold" />
                </Button>
            </Grid>

            <!-- Descripción según funcionalidad -->
            <Border Background="{DynamicResource Esri_BackgroundHoverBrush}"
                    BorderBrush="{DynamicResource Esri_BorderBrush}"
                    BorderThickness="1"
                    Padding="10"
                    CornerRadius="4"
                    Margin="0,0,0,15"
                    Visibility="{Binding EsFuncionalidad1, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock TextWrapping="Wrap" FontSize="11">
                    <Run Text="ℹ️  Esta función comprimirá la carpeta/GDB en un archivo ZIP y generará:" FontWeight="SemiBold"/>
                    <LineBreak/>
                    <Run Text="• Archivo ZIP: nombreGDB_AAAAMMDDHHMMSS.zip"/>
                    <LineBreak/>
                    <Run Text="• Archivo HASH: nombreGDB_AAAAMMDDHHMMSS_HASH.txt"/>
                    <LineBreak/>
                    <Run Text="Ambos archivos se crearán en la carpeta padre."/>
                </TextBlock>
            </Border>

            <Border Background="{DynamicResource Esri_BackgroundHoverBrush}"
                    BorderBrush="{DynamicResource Esri_BorderBrush}"
                    BorderThickness="1"
                    Padding="10"
                    CornerRadius="4"
                    Margin="0,0,0,15"
                    Visibility="{Binding EsFuncionalidad2, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock TextWrapping="Wrap" FontSize="11">
                    <Run Text="ℹ️  Esta función calculará el hash SHA256 de cada archivo en la carpeta:" FontWeight="SemiBold"/>
                    <LineBreak/>
                    <Run Text="• Se generará un archivo resumen: carpeta_AAAAMMDDHHMMSS_HASH.txt"/>
                    <LineBreak/>
                    <Run Text="• El archivo contendrá el hash de cada archivo encontrado"/>
                    <LineBreak/>
                    <Run Text="• No incluye subdirectorios (solo archivos del nivel superior)"/>
                </TextBlock>
            </Border>

            <!-- Resultado -->
            <Border Style="{StaticResource ResultBoxStyle}"
                    Visibility="{Binding ResultadoGenerar, Converter={StaticResource BooleanToVisibilityConverter}}">
                <ScrollViewer MaxHeight="150" VerticalScrollBarVisibility="Auto">
                    <TextBlock Text="{Binding ResultadoGenerar}"
                               TextWrapping="Wrap"
                               FontFamily="Consolas"
                               FontSize="11"
                               Foreground="{DynamicResource Esri_TextPrimaryBrush}"/>
                </ScrollViewer>
            </Border>

            <!-- Barra de progreso -->
            <ProgressBar Height="4"
                         Margin="0,15,0,10"
                         IsIndeterminate="True"
                         Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />

            <!-- Estado -->
            <TextBlock Text="{Binding StatusMessage}"
                       Margin="0,0,0,15"
                       TextWrapping="Wrap"
                       FontStyle="Italic"
                       Visibility="{Binding StatusMessage, Converter={StaticResource BooleanToVisibilityConverter}}" />

            <!-- Botones de Acción -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,0">
                <Button Content="Limpiar"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding LimpiarCommand}"
                        Margin="0,0,8,0" />
                
                <Button Content="GENERAR HASH"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding GenerarHashCommand}" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
